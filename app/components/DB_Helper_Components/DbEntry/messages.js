/*
 * DbEntry Messages
 *
 * This contains all the text for the DbEntry component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.components.DbEntry.header',
    defaultMessage: 'This is the DbEntry component !',
  },
});
